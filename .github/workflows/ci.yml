name: CI

on:
  push:
  pull_request:

jobs:
  someip-sd-loopback:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          python-version: "3.11"

      - name: Sync dependencies
        run: uv sync

      - name: Sanity-check imports
        run: uv run python -c "import someip_sd_demo.server, someip_sd_demo.client"

      - name: Verify IPv6 loopback is available on this runner
        run: |
          python3 -c "
          import socket
          s = socket.socket(socket.AF_INET6, socket.SOCK_DGRAM)
          s.bind(('::1', 0))
          print('AF_INET6 loopback OK:', s.getsockname())
          "

      - name: Offline SD wire round-trip check (no networking)
        # Builds the same OfferService/Subscribe entries and data
        # notifications server.py/client.py build, and confirms they
        # round-trip correctly through pysomeip's own SD serialize/parse.
        # Runs anywhere, including sandboxes with no IPv6 at all -- this is
        # the fast, deterministic part of "build and test"; the live run
        # below is the networked part.
        run: uv run python scripts/offline_roundtrip_check.py

      - name: Offline --exit-after-subscribed check (no networking)
        run: uv run python scripts/exit_after_subscribed_check.py

      - name: Diagnose raw cross-process IPv6 multicast on this runner
        # Informational only (does not fail the build): GitHub-hosted
        # runners were found NOT to deliver IPv6 multicast on `lo` between
        # processes at all, independent of pysomeip -- confirmed with this
        # exact raw socket test (join succeeds, send succeeds, recv times
        # out with zero packets, even with the multicast route added and
        # IPV6_MULTICAST_LOOP explicitly enabled). That's why the live run
        # below uses --peer-addr (unicast SD) instead of real multicast.
        # Kept here so a future runner image change that fixes this is
        # visible without digging through history.
        continue-on-error: true
        run: |
          sudo ip -6 route add ff00::/8 dev lo || true
          python3 scripts/mcast_smoke_test.py recv > recv.log 2>&1 &
          RECV_PID=$!
          sleep 1
          python3 scripts/mcast_smoke_test.py send > send.log 2>&1 &
          SEND_PID=$!
          set +e
          wait $RECV_PID
          RECV_STATUS=$?
          set -e
          kill $SEND_PID 2>/dev/null || true
          echo "=== recv.log ==="; cat recv.log
          echo "=== send.log ==="; cat send.log
          if [ "$RECV_STATUS" = "0" ]; then
            echo "::notice::multicast now works on this runner -- the live run below could switch back to it"
          else
            echo "::notice::multicast still not delivered on this runner's lo, as expected; live run uses --peer-addr"
          fi

      - name: Run server and client live, unicast SD (CI fallback for no multicast)
        # See create_unicast_sd_endpoint in common.py: real sockets, real
        # asyncio, real pysomeip SD code and timing state machine, just
        # point-to-point instead of via multicast (which this runner can't
        # deliver at all -- see the diagnostic step above). The demo's
        # default (no --peer-addr) stays multicast-based, matching the real
        # vendor deployment, for local/self-hosted use.
        run: |
          uv run sd-server --peer-addr ::1 --log-level DEBUG > server.log 2>&1 &
          echo $! > server.pid
          sleep 1
          uv run sd-client --peer-addr ::1 --log-level DEBUG > client.log 2>&1 &
          echo $! > client.pid

          # Let the full Initial-Wait/Repetition/Main-phase SD handshake
          # play out and several notification cycles (~65ms cadence) run.
          # 45s (not 20s): the first CI run to reach this point showed
          # Status subscribing/refreshing cleanly every ~3s cycle while
          # Measurements' subscribe only succeeded once, right at the very
          # end of a 20s window -- more real run time is the cheapest way
          # to tell whether that's just a slow first success or a deeper,
          # per-service asymmetry worth root-causing separately.
          sleep 45

          # Graceful shutdown first (SIGINT, same path as local Ctrl+C:
          # StopOffer / unsubscribe), then force-kill as a safety net.
          kill -INT "$(cat client.pid)" "$(cat server.pid)" 2>/dev/null || true
          sleep 2
          kill -9 "$(cat client.pid)" "$(cat server.pid)" 2>/dev/null || true

      - name: Check expected SD + data sequence in logs
        run: |
          echo "=== server.log ==="; cat server.log
          echo
          echo "=== client.log ==="; cat client.log
          echo

          fail=0
          check() {
            local file="$1" pattern="$2" desc="$3"
            if ! grep -q "$pattern" "$file"; then
              echo "::error file=$file::missing expected log line: $desc (pattern: $pattern)"
              fail=1
            else
              echo "OK: $desc"
            fi
          }

          check server.log "offering Measurements" "server offers Measurements (0x60d4)"
          check server.log "offering Status" "server offers Status (0x60d6)"
          check server.log "SubscribeEventgroup accepted" "server accepted at least one subscribe"
          check server.log "sent notification" "server sent at least one notification"

          check client.log "discovered service=0x60d4" "client discovered Measurements"
          check client.log "discovered service=0x60d6" "client discovered Status"
          check client.log "received Subscribe ACK" "client saw a SubscribeAck (pysomeip's own log line)"
          check client.log "Measurements: received notification" "client received a Measurements notification"
          check client.log "Status: received notification" "client received a Status notification"
          check client.log "static session id, as documented" "Status's static session-id quirk observed on the wire"

          exit $fail

      - name: Live check -- --exit-after-subscribed exits 0 without a StopOffer
        # Separate short-lived server/client pair (same --peer-addr unicast
        # SD fallback as the main run above, for the same no-multicast-on-lo
        # reason). Confirms the real, live behavior that scripts/
        # exit_after_subscribed_check.py can't: the server actually reaches
        # exit code 0 once subscribed, and does so WITHOUT the client ever
        # seeing a StopOffer -- which is the entire point of the feature
        # (leaving the client's SD state/multicast membership untouched for
        # a later tcpreplay of the real captured data).
        run: |
          timeout 30 uv run sd-server --peer-addr ::1 --exit-after-subscribed \
            --subscribe-timeout 20 --log-level DEBUG > server-eas.log 2>&1 &
          SERVER_PID=$!
          sleep 1
          uv run sd-client --peer-addr ::1 --log-level DEBUG > client-eas.log 2>&1 &
          CLIENT_PID=$!

          set +e
          wait "$SERVER_PID"
          SERVER_STATUS=$?
          set -e

          kill -INT "$CLIENT_PID" 2>/dev/null || true
          sleep 1
          kill -9 "$CLIENT_PID" 2>/dev/null || true

          echo "=== server-eas.log ==="; cat server-eas.log
          echo
          echo "=== client-eas.log ==="; cat client-eas.log
          echo

          fail=0
          if [ "$SERVER_STATUS" != "0" ]; then
            echo "::error::server --exit-after-subscribed exited with status $SERVER_STATUS (expected 0)"
            fail=1
          else
            echo "OK: server exited 0"
          fi
          if ! grep -q "exiting 0 for tcpreplay handoff" server-eas.log; then
            echo "::error file=server-eas.log::server did not log taking the exit-after-subscribed path"
            fail=1
          else
            echo "OK: server took the exit-after-subscribed path"
          fi
          if grep -q "shutting down: sending StopOffer" server-eas.log; then
            echo "::error file=server-eas.log::server logged the normal StopOffer shutdown path -- exit-after-subscribed must skip it"
            fail=1
          else
            echo "OK: server did not take the normal StopOffer shutdown path"
          fi

          exit $fail

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: someip-sd-logs
          path: |
            server.log
            client.log
            server-eas.log
            client-eas.log
            recv.log
            send.log

  # Exploratory: does a Docker bridge network fare any better than raw `lo`
  # for IPv6 multicast on a GitHub-hosted runner? A Docker bridge is a real
  # L2 device between two containers -- their traffic never leaves the VM's
  # own kernel netns (no hypervisor vSwitch involved), a different code path
  # than loopback multicast, which is a special-cased device in Linux. This
  # job answers that empirically instead of guessing; `continue-on-error` at
  # the job level so a "no" here never blocks the (already green, --peer-addr
  # based) primary job above.
  docker-compose-multicast:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Build images
        run: docker compose build

      - name: Smoke test -- does the Docker bridge deliver multicast at all
        # Same standalone probe as the loopback job's diagnostic step, run
        # between two throwaway containers on the compose network instead
        # of two processes on lo. Informational: logged either way, doesn't
        # fail the step by itself (the real signal is the next step).
        run: |
          set +e
          docker compose --profile smoketest up --abort-on-container-exit --exit-code-from mcast-recv mcast-recv mcast-send
          SMOKE_STATUS=$?
          set -e
          echo "=== mcast-recv logs ==="; docker compose logs mcast-recv || true
          echo "=== mcast-send logs ==="; docker compose logs mcast-send || true
          docker compose --profile smoketest down -v
          if [ "$SMOKE_STATUS" = "0" ]; then
            echo "::notice::Docker bridge multicast smoke test PASSED on this runner"
          else
            echo "::notice::Docker bridge multicast smoke test FAILED on this runner (status=$SMOKE_STATUS)"
          fi

      - name: Run the real demo over the Docker bridge, default multicast mode
        # No --peer-addr here on purpose: this is specifically testing
        # whether the demo's real, default (multicast) behavior works
        # inside Docker on this runner -- the whole point of this job.
        run: |
          docker compose up -d sd-server sd-client
          sleep 45
          docker compose logs sd-server > server.log 2>&1
          docker compose logs sd-client > client.log 2>&1
          docker compose down -v

      - name: Check expected SD + data sequence in logs
        run: |
          echo "=== server.log ==="; cat server.log
          echo
          echo "=== client.log ==="; cat client.log
          echo

          fail=0
          check() {
            local file="$1" pattern="$2" desc="$3"
            if ! grep -q "$pattern" "$file"; then
              echo "::error file=$file::missing expected log line: $desc (pattern: $pattern)"
              fail=1
            else
              echo "OK: $desc"
            fi
          }

          check server.log "offering Measurements" "server offers Measurements (0x60d4)"
          check server.log "offering Status" "server offers Status (0x60d6)"
          check server.log "SubscribeEventgroup accepted" "server accepted at least one subscribe"
          check server.log "sent notification" "server sent at least one notification"

          check client.log "discovered service=0x60d4" "client discovered Measurements"
          check client.log "discovered service=0x60d6" "client discovered Status"
          check client.log "received Subscribe ACK" "client saw a SubscribeAck (pysomeip's own log line)"
          check client.log "Measurements: received notification" "client received a Measurements notification"
          check client.log "Status: received notification" "client received a Status notification"
          check client.log "static session id, as documented" "Status's static session-id quirk observed on the wire"

          if [ "$fail" = "0" ]; then
            echo "::notice::Docker bridge multicast WORKS end-to-end on this runner -- docker-compose.yml's default (no --peer-addr) mode is CI-verified"
          else
            echo "::notice::Docker bridge multicast demo did not complete end-to-end on this runner -- see README's Docker section for the --peer-addr fallback"
          fi

          exit $fail

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: someip-sd-docker-logs
          path: |
            server.log
            client.log
